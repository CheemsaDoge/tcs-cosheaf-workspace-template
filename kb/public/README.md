# Public KB Root

This directory represents the readonly public KB root. In a real workspace, replace or mount this root from `CheemsaDoge/tcs-kb-public`.

The included seed artifact is a tiny draft bootstrap example so the template can validate before users attach the real public KB.
